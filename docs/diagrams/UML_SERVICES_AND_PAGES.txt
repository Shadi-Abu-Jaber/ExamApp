ExamFlow - UML for Services and Main Student Pages
===================================================

מטרת הדיאגרמה:
להציג את המחלקות המרכזיות בשכבת ה-Services ואת התלות בין דפי הסטודנט לשירותים.


+---------------------------------------------------+
| ConfigurationService                              |
+---------------------------------------------------+
|                                                   |
+---------------------------------------------------+
| + getApiBaseUrl(): string                         |
| + isMockMode(): boolean                           |
+---------------------------------------------------+
                 ^
                 |
                 | uses
                 |
+---------------------------------------------------+
| ApiService                                        |
+---------------------------------------------------+
| - client: AxiosInstance                           |
+---------------------------------------------------+
| + get(url, config): Promise                       |
| + post(url, payload, config): Promise             |
| + patch(url, payload, config): Promise            |
| + delete(url, config): Promise                    |
+---------------------------------------------------+
        ^                              ^
        | uses                         | uses
        |                              |
+-----------------------------+   +-----------------------------+
| StudentService              |   | SubmissionService           |
+-----------------------------+   +-----------------------------+
|                             |   |                             |
+-----------------------------+   +-----------------------------+
| + listAvailableExams()      |   | + getSubmission(id)         |
| + startExam(examId)         |   | + saveAnswer(id, payload)   |
| + getExamQuestions(examId)  |   | + submitExam(id)            |
| + listResults()             |   +-----------------------------+
| + getResult(examId)         |                 |
+-----------------------------+                 |
        |                                       |
        | uses when mock mode                   | uses when mock mode
        v                                       v
+---------------------------------------------------+
| MockDbService                                     |
+---------------------------------------------------+
| - storageKey: string                              |
+---------------------------------------------------+
| + createInitialDb(): object                       |
| + read(): object                                  |
| + write(db): void                                 |
| + reset(): object                                 |
| + listAvailableExams(): Exam[]                    |
| + getExamQuestions(examId): Question[]            |
| + startExam(examId): Submission                   |
| + getSubmission(submissionId): Submission         |
| + saveAnswer(submissionId, payload): Answer       |
| + submitExam(submissionId): object                |
| + listResults(): Submission[]                     |
| + getResult(examId): Submission                   |
+---------------------------------------------------+
        |
        | stores data in
        v
+---------------------------------------------------+
| Browser localStorage                              |
+---------------------------------------------------+


Utility Services
================

+---------------------------------------------------+
| StorageService                                    |
+---------------------------------------------------+
| - tokenKey: string                                |
+---------------------------------------------------+
| + get(key): string                                |
| + set(key, value): void                           |
| + remove(key): void                               |
| + getToken(): string                              |
| + setToken(token): void                           |
| + removeToken(): void                             |
+---------------------------------------------------+


+---------------------------------------------------+
| LoggerService                                     |
+---------------------------------------------------+
|                                                   |
+---------------------------------------------------+
| + info(message, data): void                       |
| + warn(message, data): void                       |
| + error(message, error): void                     |
+---------------------------------------------------+


+---------------------------------------------------+
| NotifyService                                     |
+---------------------------------------------------+
|                                                   |
+---------------------------------------------------+
| + getErrorMessage(error, fallback): string        |
| + success(message): string                        |
| + error(message): string                          |
+---------------------------------------------------+


Student Pages Dependencies
==========================

+---------------------------+
| StudentDashboard          |
+---------------------------+
| + loadDashboard()         |
+---------------------------+
          |
          | calls
          v
+---------------------------+
| StudentService            |
+---------------------------+


+---------------------------+
| ExamList                  |
+---------------------------+
| + loadExams()             |
+---------------------------+
          |
          | calls
          v
+---------------------------+
| StudentService            |
+---------------------------+


+---------------------------+
| ExamStart                 |
+---------------------------+
| + loadExam()              |
| + saveCurrentAnswer()     |
| + handleSubmit()          |
| + autoSubmit()            |
+---------------------------+
          |                        |
          | calls                  | calls
          v                        v
+---------------------------+   +---------------------------+
| StudentService            |   | SubmissionService         |
+---------------------------+   +---------------------------+


+---------------------------+
| StudentResults            |
+---------------------------+
| + loadResults()           |
+---------------------------+
          |
          | calls
          v
+---------------------------+
| StudentService            |
+---------------------------+


+---------------------------+
| ResultDetail              |
+---------------------------+
| + loadResult()            |
+---------------------------+
          |
          | calls
          v
+---------------------------+
| StudentService            |
+---------------------------+


Main Dependency Flow
====================

Pages
  |
  v
StudentService / SubmissionService
  |
  |-- ConfigurationService checks operation mode
  |
  |-- Real Mode: ApiService --> Backend API
  |
  |-- Mock Mode: MockDbService --> localStorage